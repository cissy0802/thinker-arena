# 三方 AI 收尾里，GPT 与 Gemini 的「本尊」总结需要各自的真实 API key。
# 不配也能跑：summarize-external.mjs 会跳过，页面显示「降级:Opus 扮演」。
# 复制本文件为 .env 并填入；.env 不要提交到 git。

OPENAI_API_KEY=
GEMINI_API_KEY=

# 默认已用两家当前旗舰高 effort 模型(已核实，截至 2026-06)。如官方 id 再变动，在此覆盖：
# OPENAI_MODEL=gpt-5.5                    # OpenAI 旗舰(极限可用 gpt-5.5-pro)
# OPENAI_REASONING_EFFORT=high           # high 或 xhigh；改用非推理模型时留空
# GEMINI_MODEL=gemini-3.1-pro-preview    # Gemini 3.1 Pro(注意 -preview 后缀)，不是 flash
